import React from 'react';
import { Text } from 'react-native';

import Card from './Card';

const Translator = () => (
  <Card styles={{ height: 192, marginBottom: 32 }}>
    <Text>Translator</Text>
  </Card>
);

export default Translator;
